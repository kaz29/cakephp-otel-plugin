services:
  otel-database:
    container_name: otel-database
    image: postgres:13.4-alpine
    volumes:
      - otel-database-data:/var/lib/postgresql/data:cached
    environment:
      POSTGRES_PASSWORD: "Passw0rd"
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=ja_JP.UTF-8"
      POSTGRES_USER: "webapp"

  otel-app:
    container_name: otel-app
    build:
      context: ./
      dockerfile: Dockerfile
    depends_on:
      - otel-database
    environment:
      DEBUG: 1
      APP_ENV: development
      DB_HOST: otel-database
      DB_PORT: 5432
      DB_NAME: webapp
      DB_USER: webapp
      DB_PASSWORD: Passw0rd
    volumes:
      - ./:/srv/app
  otel-app-8.3:
    container_name: otel-app-8.3
    build:
      context: ./
      dockerfile: Dockerfile.8.3
    depends_on:
      - otel-database
    environment:
      DEBUG: 1
      APP_ENV: development
      DB_HOST: otel-database
      DB_PORT: 5432
      DB_NAME: webapp
      DB_USER: webapp
      DB_PASSWORD: Passw0rd
    volumes:
      - ./:/srv/app

volumes:
  otel-database-data:
